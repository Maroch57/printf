#include "main.h"

/**
 * get_modifier - to set all needed modifiers
 * @c: modifier to set
 * @mod: pointer to modifiers
 *
 * Return: 1 if a modifier is set
 * 0 Otherwise
 */
int get_modifier(char c, mod_t *mod)
{
	(void) c;
	(void) mod;
	return (0);
}

